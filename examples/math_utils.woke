// Math utilities module

to square(n) {
    give back n * n;
}

to cube(n) {
    give back n * n * n;
}

to factorial(n) {
    when n <= 1 {
        give back 1;
    }

    give back n * factorial(n - 1);
}
