// SPDX-License-Identifier: MPL-2.0
// Simple record test

#verbose on;

type Point = {
    x: Int,
    y: Int
};

to main() {
    remember p = Point {
        x: 10,
        y: 20
    };

    print(toString(p.x));
    print(toString(p.y));

    give back 0;
}
