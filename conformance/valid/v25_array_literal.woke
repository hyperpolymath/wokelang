// SPDX-License-Identifier: MPL-2.0
// Conformance: array literal binding

to main() {
    remember items = [1, 2, 3];
    remember names = ["alpha", "beta", "gamma"];
    remember empty = [];

    print(items);
    print(names);
    give back 0;
}
