// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Vscode from "vscode";
import * as Js_dict from "rescript/lib/es6/js_dict.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as $$Node from "vscode-languageclient/node";

var client = {
  contents: undefined
};

function startLanguageClient(_context) {
  var config = Vscode.workspace.getConfiguration("wokelang");
  var path = config.get("serverPath");
  var serverPath = path !== undefined ? path : "woke-lsp";
  var serverOptions_run = {
    command: serverPath,
    transport: 0
  };
  var serverOptions_debug = {
    command: serverPath,
    transport: 0,
    options: {
      env: Js_dict.fromArray([[
              "RUST_BACKTRACE",
              "1"
            ]])
    }
  };
  var serverOptions = {
    run: serverOptions_run,
    debug: serverOptions_debug
  };
  var clientOptions_documentSelector = [{
      scheme: "file",
      language: "wokelang"
    }];
  var clientOptions_synchronize = {
    fileEvents: Vscode.workspace.createFileSystemWatcher("**/*.woke")
  };
  var clientOptions = {
    documentSelector: clientOptions_documentSelector,
    synchronize: clientOptions_synchronize
  };
  var languageClient = new $$Node.LanguageClient("wokelang", "WokeLang Language Server", serverOptions, clientOptions);
  client.contents = Caml_option.some(languageClient);
  languageClient.start();
}

function restartServer(context) {
  var c = client.contents;
  if (c !== undefined) {
    Caml_option.valFromOption(c).stop();
    return startLanguageClient(context);
  } else {
    return startLanguageClient(context);
  }
}

function activate(context) {
  startLanguageClient(context);
  var restartCmd = Vscode.commands.registerCommand("wokelang.restartServer", (function () {
          return new Promise((function (resolve, param) {
                        restartServer(context);
                        resolve();
                      }));
        }));
  context.subscriptions.push(restartCmd);
}

function deactivate() {
  var c = client.contents;
  if (c !== undefined) {
    return Caml_option.some(Caml_option.valFromOption(c).stop());
  }
  
}

export {
  client ,
  startLanguageClient ,
  restartServer ,
  activate ,
  deactivate ,
}
/* vscode Not a pure module */
